import { Paths } from '../../routes/pathes';

export const menuItemsKeys: Record<string, string> = {
    [Paths.CALENDAR_PAGE]: 'calendar',
    '/trains': 'trains',
    '/trophy': 'trophy',
    '/profile': 'profile',
};
